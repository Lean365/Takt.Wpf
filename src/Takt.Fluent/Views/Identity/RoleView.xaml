<UserControl x:Class="Takt.Fluent.Views.Identity.RoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:Takt.Fluent.Controls"
             xmlns:local="clr-namespace:Takt.Fluent.Localization"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1280">
    <controls:TaktDataGrid ItemsSource="{Binding Roles}"
                                SelectedItem="{Binding SelectedRole, Mode=TwoWay}"
                                QueryKeyword="{Binding Keyword, Mode=TwoWay}"
                                QueryPlaceholder="{local:Loc Key=common.placeholder.keywordHint, Param0Key=Identity.Role.Keyword}"
                                QueryButtonText="{local:Loc Key=common.search}"
                                ResetButtonText="{local:Loc Key=common.reset}"
                                EmptyText="{Binding EmptyMessage}"
                                ShowToolbarCreate="True"
                                ShowToolbarUpdate="True"
                                ShowToolbarDelete="True"
                                ShowToolbarImport="True"
                                ShowToolbarExport="True"
                                ShowAdvancedQueryButton="False"
                                ImportCommand="{Binding ImportCommand}"
                                ExportCommand="{Binding ExportCommand}"
                                DownloadTemplateCommand="{Binding DownloadTemplateCommand}"
                                PageIndex="{Binding PageIndex, Mode=TwoWay}"
                                PageSize="{Binding PageSize, Mode=TwoWay}"
                                TotalCount="{Binding TotalCount}"
                                QueryCommand="{Binding QueryCommand}"
                                ResetCommand="{Binding ResetCommand}"
                                CreateCommand="{Binding CreateCommand}"
                                UpdateCommand="{Binding UpdateCommand}"
                                DeleteCommand="{Binding DeleteCommand}"
                                ShowRowCreate="False"
                                ShowRowUpdate="True"
                                ShowRowDelete="True"
                                RowCreateCommand="{Binding CreateCommand}"
                                RowUpdateCommand="{Binding UpdateCommand}"
                                RowDeleteCommand="{Binding DeleteCommand}"
                                PageChangedCommand="{Binding PageChangedCommand}">
        <controls:TaktDataGrid.Columns>
            <controls:TaktDataGridColumnDefinition Header="{local:Loc Key=Identity.Role.RoleName}"
                                                        BindingPath="RoleName"
                                                        Width="200" />
            <controls:TaktDataGridColumnDefinition Header="{local:Loc Key=Identity.Role.RoleCode}"
                                                        BindingPath="RoleCode"
                                                        Width="150" />
            <controls:TaktDataGridColumnDefinition Header="{local:Loc Key=Identity.Role.Description}"
                                                        BindingPath="Description"
                                                        Width="300" />
            <controls:TaktDataGridColumnDefinition Header="{local:Loc Key=Identity.Role.DataScope}"
                                                        BindingPath="DataScope"
                                                        Width="150" />
            <controls:TaktDataGridColumnDefinition Header="{local:Loc Key=Identity.Role.UserCount}"
                                                        BindingPath="UserCount"
                                                        Width="120" />
            <controls:TaktDataGridColumnDefinition Header="{local:Loc Key=Identity.Role.OrderNum}"
                                                        BindingPath="OrderNum"
                                                        Width="120" />
            <controls:TaktDataGridColumnDefinition Header="{local:Loc Key=Identity.Role.RoleStatus}"
                                                        BindingPath="RoleStatus"
                                                        Width="120" />
            <controls:TaktDataGridColumnDefinition Header="{local:Loc Key=common.audit.Remarks}"
                                                        BindingPath="Remarks"
                                                        Width="200" />
            <controls:TaktDataGridColumnDefinition Header="{local:Loc Key=common.audit.CreatedTime}"
                                                        BindingPath="CreatedTime"
                                                        Width="200"
                                                        StringFormat="{}{0:yyyy-MM-dd HH:mm:ss}" />
        </controls:TaktDataGrid.Columns>
    </controls:TaktDataGrid>
</UserControl>

