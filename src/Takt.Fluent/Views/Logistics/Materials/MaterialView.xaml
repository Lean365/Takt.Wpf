<UserControl x:Class="Takt.Fluent.Views.Logistics.Materials.MaterialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:Takt.Fluent.Controls"
             xmlns:local="clr-namespace:Takt.Fluent.Localization"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1280"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">
    <Grid HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch">
        <controls:TaktDataGrid ItemsSource="{Binding ProdMaterials}"
                               HorizontalAlignment="Stretch"
                               VerticalAlignment="Stretch"
                                SelectedItem="{Binding SelectedProdMaterial, Mode=TwoWay}"
                                QueryKeyword="{Binding Keyword, Mode=TwoWay}"
                                QueryPlaceholder="{local:Loc Key=common.placeholder.keywordHint, Param0Key=Logistics.Material.Keyword}"
                                ShowToolbarCreate="True"
                                ShowToolbarUpdate="True"
                                ShowToolbarDelete="True"
                                ShowToolbarImport="True"
                                ShowToolbarExport="True"
                                CreateCommand="{Binding CreateCommand}"
                                UpdateCommand="{Binding UpdateCommand}"
                                DeleteCommand="{Binding DeleteCommand}"
                                ImportCommand="{Binding ImportCommand}"
                                ExportCommand="{Binding ExportCommand}"
                                DownloadTemplateCommand="{Binding DownloadTemplateCommand}"
                                RowUpdateCommand="{Binding UpdateCommand}"
                                RowDeleteCommand="{Binding DeleteCommand}"
                                PageIndex="{Binding PageIndex, Mode=TwoWay}"
                                PageSize="{Binding PageSize, Mode=TwoWay}"
                                TotalCount="{Binding TotalCount}"
                                QueryCommand="{Binding QueryCommand}"
                                ResetCommand="{Binding ResetCommand}"
                                PageChangedCommand="{Binding PageChangedCommand}"
                                EmptyText="{Binding EmptyMessage}">
            <controls:TaktDataGrid.Columns>
                <controls:TaktDataGridColumnDefinition Header="{local:Loc Key=Logistics.Material.MaterialCode}" BindingPath="MaterialCode" Width="150" />
                <controls:TaktDataGridColumnDefinition Header="{local:Loc Key=Logistics.Material.MaterialDescription}" BindingPath="MaterialDescription" Width="200" />
                <controls:TaktDataGridColumnDefinition Header="{local:Loc Key=Logistics.Material.MaterialType}" BindingPath="MaterialType" Width="120" />
                <controls:TaktDataGridColumnDefinition Header="{local:Loc Key=Logistics.Material.Plant}" BindingPath="Plant" Width="100" />
                <controls:TaktDataGridColumnDefinition Header="{local:Loc Key=Logistics.Material.BaseUnit}" BindingPath="BaseUnit" Width="100" />
                <controls:TaktDataGridColumnDefinition Header="{local:Loc Key=Logistics.Material.StockQuantity}" BindingPath="StockQuantity" Width="120" />
            </controls:TaktDataGrid.Columns>
        </controls:TaktDataGrid>
    </Grid>
</UserControl>

