<UserControl x:Class="Takt.Fluent.Views.Logistics.Visitors.DigitalSignageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Takt.Fluent.Localization"
             xmlns:helpers="clr-namespace:Takt.Fluent.Helpers"
             xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
             mc:Ignorable="d"
             d:DesignHeight="1080"
             d:DesignWidth="1920">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <helpers:InverseBoolConverter x:Key="InverseBoolConverter"/>
        
        <!-- 欢迎牌样式 -->
        <Style x:Key="WelcomeCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignDivider}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="40"/>
            <Setter Property="Margin" Value="40"/>
        </Style>

        <Style x:Key="WelcomeTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="72"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,40"/>
        </Style>

        <Style x:Key="WelcomeCompanyStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="48"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,30"/>
        </Style>

        <Style x:Key="WelcomeDetailStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="36"/>
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <Style x:Key="WelcomeHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="72"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,40"/>
        </Style>

        <Style x:Key="WelcomeFooterStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="48"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,40,0,0"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- 访客信息欢迎牌 -->
        <Border Style="{StaticResource WelcomeCardStyle}"
                Visibility="{Binding ShowVisitorInfo, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center">
                <!-- Header: 热烈欢迎 -->
                <TextBlock Text="{local:Loc Key=Logistics.DigitalSignage.Header}" 
                           Style="{StaticResource WelcomeHeaderStyle}"/>

                <!-- 公司名称 -->
                <TextBlock Text="{Binding CurrentVisitor.CompanyName}" 
                           Style="{StaticResource WelcomeCompanyStyle}"/>

                <!-- 访客详情列表 -->
                <ItemsControl ItemsSource="{Binding CurrentVisitorDetails}" 
                              Margin="0,20,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" 
                                        HorizontalAlignment="Center" 
                                        Margin="0,10,0,10">
                                <TextBlock Text="{Binding Department}" 
                                           Style="{StaticResource WelcomeDetailStyle}" 
                                           Margin="0,0,20,0"/>
                                <TextBlock Text="{Binding Name}" 
                                           Style="{StaticResource WelcomeDetailStyle}" 
                                           Margin="0,0,20,0"/>
                                <TextBlock Text="{Binding Position}" 
                                           Style="{StaticResource WelcomeDetailStyle}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Footer: 莅临指导 -->
                <TextBlock Text="{local:Loc Key=Logistics.DigitalSignage.Footer}" 
                           Style="{StaticResource WelcomeFooterStyle}"/>
            </StackPanel>
        </Border>

        <!-- 广告视频 -->
        <Grid Visibility="{Binding ShowVisitorInfo, Converter={StaticResource InverseBoolConverter}}">
            <vlc:VideoView x:Name="AdVideoPlayer"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"/>
        </Grid>
    </Grid>
</UserControl>

