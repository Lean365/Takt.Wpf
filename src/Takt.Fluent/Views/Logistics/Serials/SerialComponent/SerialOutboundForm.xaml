<Window x:Class="Takt.Fluent.Views.Logistics.Serials.SerialComponent.SerialOutboundForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Takt.Fluent.Localization"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:helpers="clr-namespace:Takt.Fluent.Helpers"
        xmlns:controls="clr-namespace:Takt.Fluent.Controls"
        mc:Ignorable="d"
        d:DesignHeight="400"
        d:DesignWidth="600"
        Title="{Binding Title}" 
        SizeToContent="Manual" 
        WindowStartupLocation="Manual"
        ResizeMode="CanResize" 
        Background="{DynamicResource MaterialDesignPaper}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <helpers:EmptyToVisibilityConverter x:Key="EmptyToVisibilityConverter"/>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            <Setter Property="TextAlignment" Value="Right"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Margin="0,0,0,16">
            <StackPanel Margin="16">
                <!-- 仕向地 -->
                <StackPanel Margin="0,0,0,8" MinHeight="56">
                    <Grid MinHeight="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,12,0">
                            <Run Text="*" Foreground="{DynamicResource SystemFillColorCriticalBrush}"/>
                            <Run Text="{local:Loc Key=Logistics.Serials.ProdSerial.DestCode}"/>
                        </TextBlock>
                        <ComboBox Grid.Column="1"
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                  materialDesign:HintAssist.Hint="{local:Loc Key=Logistics.Serials.ProdSerial.DestCodeHint}"
                                  materialDesign:TextFieldAssist.HasClearButton="True"
                                  ItemsSource="{Binding DestCodeOptions}"
                                  DisplayMemberPath="DataLabel"
                                  IsEditable="True">
                            <ComboBox.SelectedItem>
                                <Binding
                                    Mode="TwoWay"
                                    Path="SelectedDestCodeOption"
                                    UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <controls:NotEmptyValidationRule
                                            ValidatesOnTargetUpdated="True"/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </ComboBox.SelectedItem>
                        </ComboBox>
                    </Grid>
                </StackPanel>

                <!-- 目的地港口 -->
                <StackPanel Margin="0,0,0,8" MinHeight="56">
                    <Grid MinHeight="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,12,0">
                            <Run Text="*" Foreground="{DynamicResource SystemFillColorCriticalBrush}"/>
                            <Run Text="{local:Loc Key=Logistics.Serials.ProdSerialOutbound.DestPort}"/>
                        </TextBlock>
                        <ComboBox Grid.Column="1"
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                  materialDesign:HintAssist.Hint="{local:Loc Key=Logistics.Serials.ProdSerialOutbound.DestPortHint}"
                                  materialDesign:TextFieldAssist.HasClearButton="True"
                                  ItemsSource="{Binding DestPortOptions}"
                                  DisplayMemberPath="DataLabel"
                                  IsEditable="True">
                            <ComboBox.SelectedItem>
                                <Binding
                                    Mode="TwoWay"
                                    Path="SelectedDestPortOption"
                                    UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <controls:NotEmptyValidationRule
                                            ValidatesOnTargetUpdated="True"/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </ComboBox.SelectedItem>
                        </ComboBox>
                    </Grid>
                </StackPanel>

                <!-- 出库单号 -->
                <StackPanel Margin="0,0,0,8" MinHeight="56">
                    <Grid MinHeight="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,12,0">
                            <Run Text="*" Foreground="{DynamicResource SystemFillColorCriticalBrush}"/>
                            <Run Text="{local:Loc Key=Logistics.Serials.ProdSerialOutbound.OutboundNo}"/>
                        </TextBlock>
                        <TextBox Grid.Column="1" 
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="{local:Loc Key=Logistics.Serials.ProdSerialOutbound.OutboundNoHint}"
                                 materialDesign:HintAssist.HelperText="{Binding OutboundNoError}"
                                 materialDesign:TextFieldAssist.DecorationVisibility="Collapsed"
                                 MinHeight="32"
                                 Padding="12,6"
                                 FontSize="13"
                                 VerticalAlignment="Center"
                                 VerticalContentAlignment="Center"
                                 Foreground="{DynamicResource MaterialDesignBody}"
                                 Text="{Binding OutboundNo, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>
                    <TextBlock Grid.Column="1" 
                               Style="{StaticResource ErrorTextStyle}" 
                               Text="{Binding OutboundNoError}"
                               Visibility="{Binding OutboundNoError, Converter={StaticResource EmptyToVisibilityConverter}}"/>
                </StackPanel>

                <!-- 出库日期 -->
                <StackPanel Margin="0,0,0,8" MinHeight="56">
                    <Grid MinHeight="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,12,0">
                            <Run Text="*" Foreground="{DynamicResource SystemFillColorCriticalBrush}"/>
                            <Run Text="{local:Loc Key=Logistics.Serials.ProdSerialOutbound.OutboundDate}"/>
                        </TextBlock>
                        <DatePicker Grid.Column="1" 
                                    SelectedDate="{Binding OutboundDate, UpdateSourceTrigger=PropertyChanged}"
                                    materialDesign:HintAssist.Hint="{local:Loc Key=Logistics.Serials.ProdSerialOutbound.OutboundDateHint}"
                                    Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
                    </Grid>
                    <TextBlock Grid.Column="1" 
                               Style="{StaticResource ErrorTextStyle}" 
                               Text="{Binding OutboundDateError}"
                               Visibility="{Binding OutboundDateError, Converter={StaticResource EmptyToVisibilityConverter}}"/>
                </StackPanel>

                <!-- 完整序列号（扫描输入） -->
                <StackPanel Margin="0,0,0,8" MinHeight="80">
                    <Grid MinHeight="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,12,0">
                            <Run Text="*" Foreground="{DynamicResource SystemFillColorCriticalBrush}"/>
                            <Run Text="{local:Loc Key=Logistics.Serials.ProdSerialOutbound.FullSerialNumber}"/>
                        </TextBlock>
                        <TextBox x:Name="FullSerialNumberTextBox"
                                 Grid.Column="1" 
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="{local:Loc Key=Logistics.Serials.ProdSerialOutbound.FullSerialNumberHint}"
                                 materialDesign:HintAssist.HelperText="{Binding FullSerialNumberError}"
                                 materialDesign:TextFieldAssist.DecorationVisibility="Collapsed"
                                 MinHeight="36"
                                 Padding="16,10"
                                 FontSize="18"
                                 VerticalAlignment="Center"
                                 VerticalContentAlignment="Center"
                                 Foreground="{DynamicResource MaterialDesignBody}"
                                 Text="{Binding FullSerialNumber, UpdateSourceTrigger=PropertyChanged}"
                                 FontWeight="SemiBold"
                                 AcceptsReturn="False"
                                 TextInput="FullSerialNumberTextBox_TextInput"/>
                    </Grid>
                    <TextBlock Grid.Column="1" 
                               Style="{StaticResource ErrorTextStyle}" 
                               Text="{Binding FullSerialNumberError}"
                               Visibility="{Binding FullSerialNumberError, Converter={StaticResource EmptyToVisibilityConverter}}"/>
                    <TextBlock Grid.Column="1" 
                               Margin="0,8,0,0"
                               FontSize="12"
                               Foreground="{DynamicResource MaterialDesignBodyLight}"
                               Text="{local:Loc Key=Logistics.Serials.ProdSerialOutbound.FullSerialNumberHint}"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button IsCancel="True" 
                    Command="{Binding CancelCommand}"
                    Content="{local:Loc Key=common.button.cancel}"
                    Style="{StaticResource DefaultInfo}"/>
        </StackPanel>
    </Grid>
</Window>

