<UserControl x:Class="Takt.Fluent.Views.Routine.RoutinePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Takt.Fluent.Controls"
             xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
             xmlns:local="clr-namespace:Takt.Fluent.Localization"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid VerticalAlignment="Top"
          HorizontalAlignment="Stretch"
          Margin="0">
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/takt_header.png" 
                        Opacity="0.35"
                        Stretch="UniformToFill"
                        TileMode="None"
                        AlignmentX="Center"
                        AlignmentY="Center" />
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.RowSpan="2" Background="{DynamicResource LayerFillColorDefaultBrush}" Opacity="0.6" />
        <controls:TaktPageHeader
            Grid.Row="0"
            Margin="0,0,0,40"
            Title="{Binding ViewModel.PageTitle}"
            Description="{Binding ViewModel.PageDescription}" />

        <ItemsControl
            Grid.Row="1"
            Margin="-12,0,0,0"
            AutomationProperties.Name="Items in group"
            ItemsSource="{Binding ViewModel.NavigationCards}"
            Focusable="False"
            ItemsPanel="{StaticResource WrapPanelTemplate}"
            ItemTemplate="{StaticResource NavigationCardTemplate}" />
    </Grid>
</UserControl>
