<UserControl x:Class="Takt.Fluent.Views.Routine.LocalizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:Takt.Fluent.Controls"
             xmlns:local="clr-namespace:Takt.Fluent.Localization"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1280">
    <controls:TaktDataGrid ItemsSource="{Binding TranslationKeys}"
                                SelectedItem="{Binding SelectedTranslationKey, Mode=TwoWay}"
                                Columns="{Binding Columns}"
                                QueryKeyword="{Binding TranslationKeyKeyword, Mode=TwoWay}"
                                QueryPlaceholder="{local:Loc Key=common.placeholder.keywordHint, Param0Key=Routine.Localization.TranslationKeyword}"
                                QueryButtonText="{local:Loc Key=common.search}"
                                ResetButtonText="{local:Loc Key=common.reset}"
                                EmptyText="{Binding EmptyMessage}"
                                ShowQueryArea="True"
                                ShowToolbarCreate="True"
                                ShowToolbarUpdate="True"
                                ShowToolbarDelete="True"
                                ShowToolbarImport="True"
                                ShowToolbarExport="True"
                                ShowAdvancedQueryButton="False"
                                ImportCommand="{Binding ImportCommand}"
                                ExportCommand="{Binding ExportCommand}"
                                DownloadTemplateCommand="{Binding DownloadTemplateCommand}"
                                ShowRowCreate="False"
                                ShowRowUpdate="True"
                                ShowRowDelete="True"
                                QueryCommand="{Binding QueryTranslationKeyCommand}"
                                ResetCommand="{Binding ResetTranslationKeyCommand}"
                                CreateCommand="{Binding CreateTranslationKeyCommand}"
                                UpdateCommand="{Binding UpdateTranslationKeyCommand}"
                                DeleteCommand="{Binding DeleteTranslationKeyCommand}"
                                RowUpdateCommand="{Binding UpdateTranslationKeyCommand}"
                                RowDeleteCommand="{Binding DeleteTranslationKeyCommand}"
                                PageIndex="{Binding TranslationKeyPageIndex, Mode=TwoWay}"
                                PageSize="{Binding TranslationKeyPageSize, Mode=TwoWay}"
                                TotalCount="{Binding TranslationKeyTotalCount}"
                                PageChangedCommand="{Binding PageChangedCommand}" />
</UserControl>

