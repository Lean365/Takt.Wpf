<UserControl x:Class="Takt.Fluent.Controls.TaktTransfer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="400"
             d:DesignWidth="600">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- ListBox 样式：只设置必要属性，使用原生样式 -->
        <Style x:Key="TransferListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}"/>
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignDivider}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        </Style>
        
        <!-- ListBoxItem 样式：只设置必要属性，使用原生样式 -->
        <Style x:Key="TransferListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource MaterialDesignCardBackground}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryHueMidBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidForegroundBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧：未分配列表 -->
        <Grid Grid.Column="0" Margin="0,0,8,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" 
                       Text="{Binding LeftTitle, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                       Margin="0,0,0,8"/>
            
            <ListBox Grid.Row="1"
                     x:Name="LeftListBox"
                     ItemsSource="{Binding LeftItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     DisplayMemberPath="{Binding DisplayMemberPath, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     ItemTemplate="{Binding ItemTemplate, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     SelectionMode="Extended"
                     Style="{StaticResource TransferListBoxStyle}"
                     ItemContainerStyle="{StaticResource TransferListBoxItemStyle}"
                     MaxHeight="400"/>
        </Grid>

        <!-- 中间：操作按钮 -->
        <StackPanel Grid.Column="1" 
                    VerticalAlignment="Center" 
                    Margin="8,0">
            <Button x:Name="MoveRightButton"
                    Content="&gt;"
                    ToolTip="{Binding MoveRightToolTip, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Style="{StaticResource MaterialDesignIconButton}"
                    Width="40"
                    Height="40"
                    Margin="0,0,0,8"
                    Click="MoveRightButton_Click"
                    IsEnabled="{Binding CanMoveRight, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            
            <Button x:Name="MoveAllRightButton"
                    Content="&gt;&gt;"
                    ToolTip="{Binding MoveAllRightToolTip, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Style="{StaticResource MaterialDesignIconButton}"
                    Width="40"
                    Height="40"
                    Margin="0,0,0,8"
                    Click="MoveAllRightButton_Click"
                    IsEnabled="{Binding CanMoveAllRight, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            
            <Button x:Name="MoveLeftButton"
                    Content="&lt;"
                    ToolTip="{Binding MoveLeftToolTip, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Style="{StaticResource MaterialDesignIconButton}"
                    Width="40"
                    Height="40"
                    Margin="0,0,0,8"
                    Click="MoveLeftButton_Click"
                    IsEnabled="{Binding CanMoveLeft, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            
            <Button x:Name="MoveAllLeftButton"
                    Content="&lt;&lt;"
                    ToolTip="{Binding MoveAllLeftToolTip, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Style="{StaticResource MaterialDesignIconButton}"
                    Width="40"
                    Height="40"
                    Click="MoveAllLeftButton_Click"
                    IsEnabled="{Binding CanMoveAllLeft, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </StackPanel>

        <!-- 右侧：已分配列表 -->
        <Grid Grid.Column="2" Margin="8,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" 
                       Text="{Binding RightTitle, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                       Margin="0,0,0,8"/>
            
            <ListBox Grid.Row="1"
                     x:Name="RightListBox"
                     ItemsSource="{Binding RightItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     DisplayMemberPath="{Binding DisplayMemberPath, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     ItemTemplate="{Binding ItemTemplate, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     SelectionMode="Extended"
                     Style="{StaticResource TransferListBoxStyle}"
                     ItemContainerStyle="{StaticResource TransferListBoxItemStyle}"
                     MaxHeight="400"/>
        </Grid>
    </Grid>
</UserControl>

